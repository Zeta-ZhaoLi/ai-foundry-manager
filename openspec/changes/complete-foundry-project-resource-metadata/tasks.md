# Tasks

- [x] Update the exported Foundry project resource builder so it includes `location`, `properties.displayName`, and `properties.description`.
- [x] Update deployment export spec coverage to require the completed project resource metadata contract.
- [x] Add or update template generation tests to assert the full project resource shape.
- [x] Validate proposal artifacts with `openspec validate complete-foundry-project-resource-metadata --strict`.
